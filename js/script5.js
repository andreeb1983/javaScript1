//Récapitulatif

// const est une constante accessible uniquement en lecture dans le contexte global et modifiable dans un contexte local.

//les conditions if...else if else permettent de définire une condition pour laquelle ont souhaite exécuter un code particulier.C'est un bloc de code local.

//les opérateurs logiques: && (AND), || (OR), ! (NOT) -> NOT transforme ce qui est vrai en faux et ce qui est faux en vrai (true->false et false->true).
//-> les opérateurs logiques sont généralements utilisés avec des valeurs booléennes et renverront des booléens à l'exception de AND  (&&) et OR (||).

// les variables (var) permettentde créer un objet (boîte dans laquelle on stocke des données) 
// ex: var boite = ["donnee1","donnee2", "donnee3", "donnee4"];
// Il existe des variables à portées globale et locale.



